#pragma once

#include "graphics_api/render_pass.hpp"
#include "graphics_api/framebuffer.hpp"

/*
namespace PG
{
namespace Gfx
{

enum RenderPasses
{
    GFX_RENDER_PASS_DEPTH_PREPASS,
    GFX_RENDER_PASS_LIT,
    GFX_RENDER_PASS_SKYBOX,
    GFX_RENDER_PASS_POST_PROCESS,

    GFX_RENDER_PASS_COUNT
};

bool InitRenderPasses();

void FreeRenderPasses();

RenderPass* GetRenderPass( RenderPasses renderPassKey );

Framebuffer* GetFramebuffer( RenderPasses renderPassKey );

} // namespace Gfx
} // namespace PG
*/